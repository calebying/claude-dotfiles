# Global Instructions

## Prompting Behavior

Before making any changes — editing files, writing files, modifying system settings (PATH, registry, config), or running PowerShell/Bash commands that mutate state — always state what you're about to do and confirm with the user first.

Do NOT prompt before running read-only information-gathering commands (e.g., checking versions, listing directories, reading files, querying system state).
